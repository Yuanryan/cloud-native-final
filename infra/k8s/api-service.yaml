apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: safety-demo
  labels:
    app: safety-api
spec:
  selector:
    app: safety-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
